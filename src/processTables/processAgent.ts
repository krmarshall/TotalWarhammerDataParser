import { TableRecord } from '../interfaces/GlobalDataInterface';

const processAgent = (agent: TableRecord, cultureKey: string, subcultureKey: string, factionKey: string) => {
  // To Do
};

export default processAgent;
