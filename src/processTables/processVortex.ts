import { TableRecord } from '../interfaces/GlobalDataInterface';
import { VortexInterface } from '../interfaces/ProcessedTreeInterface';

const processVortex = (vortex: TableRecord): VortexInterface => {
  return {};
};

export default processVortex;
